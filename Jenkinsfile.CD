pipeline {
    agent { label 'deploymentNode' }
    //agent any
    stages {
        stage('Deploy') {
            steps {
                sh '''
                    docker compose -f compose-release.yml up -d
                '''
            }
        }
    }
}
